---
description: DOM Racer session context and handoff state for the next AI session
alwaysApply: true
---
# DOM Racer - Next Session Context

Continue work on the DOM Racer browser extension in `/Users/ivan.seredkin/_projects/dom-racer`.

First, read:
- `SOUL.md` (project context for AI agents)
- `README.md`
- `.cursor/rules/phases.mdc` (development phases and learnings)
- `src/game/Game.ts`
- `src/game/gameConfig.ts`
- `src/game/gameStateTypes.ts`

## Current State

Phases 1-19 complete. 100 smoke tests across 9 files. All passing, clean build. No pending phases. Production readiness audit done.

Key decisions:
- 9 specials: bonus, magnet, invert, ghost, jackpot, blur, oil_slick, reverse, mystery
- Overgrowth: grass → bush → tree single escalation ladder
- Special pickup shapes: helpful=rounded square+chevron, harmful=diamond, mystery=circle, jackpot=star
- Form controls scanned as slow zones + overgrowth anchors
- HUD controls: `WASD/ARROWS` left, `SPACE NITRO` right, line 2 fixed-position columns
- Spawn strongly center-biased. All score increments multiples of 10.
- Police helicopter from chase #3+: airborne movement, rotor chop audio, amber "CHOPPER" warning, searchlight, body flash lights
- Train encounter: spawns at viewport center (random horizontal/vertical axis), Caltrain-style sprite, horn + rumble audio, yellow "TRAIN" warning, "HIT BY TRAIN" game over, ghost immunity, max 1 per run
- Sprite showcase (Shift+D): helicopter, train, clickable sound test panel with active-state indicators
- Daily modifiers: 4 types (DOUBLE_COINS, FAST_POLICE, EXTRA_SPECIALS, EARLY_OVERGROWTH), first-play protection (no modifier when lifetime score is 0)
- First police arrives at ~8-10s (fast start)
- Unpause grace: 1.5s extension on all timers when tab regains focus
- Pixel-art branding: horizontal stepped color bands (dark navy → teal) across icon, cover, tile

## Essential Context

- `window.__domRacerDebug` must not exist in source/build.
- Centralized tuning: `gameConfig.ts` groups (TIMING, TOAST, COINS, SPECIALS, EFFECTS, JACKPOT, POLICE, PLANE, TRAIN, ENCOUNTER, OVERGROWTH, NEAR_MISS, OBJECTIVES, PLAYER, VEHICLES).
- Runtime modules are pure functions: logic in `*Runtime.ts`, orchestration in `Game.ts`.
- Sprite files in `src/game/sprites/`; consumers import from barrel `./sprites`.
- ESLint + Prettier. Scripts: `lint`, `lint:fix`, `format`, `format:check`.
- 100 smoke tests across 9 files. `npm run test` / `npm run build` must pass.
- Police chase lifecycle in `encounterRuntime.ts` — base duration 4000-6000ms, escalates with run time. From chase #3+, variant switches to helicopter.
- Overgrowth: `OvergrowthStage = 'grass' | 'bush' | 'tree'`. Anchors from obstacles + slowZones.
- All score increments multiples of 10. Near-miss is visual-only (no score).
- Extension popup: static HTML in `public/popup.html`. No JS build step.
- Adaptive sprite contrast: `applyAdaptiveShadow()`/`clearAdaptiveShadow()` in `spriteHelpers.ts`.
- Branding: pixel-art SVGs in `branding/`, PNGs generated via `generate_assets.py` using `rsvg-convert`.
- Game toggle: Shift+Space (or Shift+Backtick). Popup "Start Game" button also toggles.

## Constraints To Preserve

- No screen shake
- Playful, readable, non-violent tone
- Keep controls/core loop stable
- Only specials expire over time, not regular coins
- Game settings (sound, vehicle) are global, not per-tab
